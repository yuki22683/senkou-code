- [x] fstringの選択肢がf'○{○○}○'が一つの選択肢になっている演習と、ばらばらになっている演習が存在する。ばらばらになる方にそろえて。
- [x] クリックやタップで現在の行のカーソル位置を有効な場所に移動できるように修正して。
- [x] 全言語のレッスン一覧画面でログアウトしているときにレッスンはありません。ではなく"レッスンを始めるにはログインが必要です。"と表示するように修正して。
- [x] コンソールに結果を出力するときに縦スクロールを最下部までスクロールさせるように修正して。
- [x] 選択肢の最上位の行のボタンの文字は途切れないようにして、収まらない場合はボタンの幅を大きくして、最上位の行のボタンがテキストエディタの幅に収まらない場合は、複数行に分割するように修正して。
- [x] 解説スライドのイラストと解説文の内容が不一致の箇所がある。イラストに解説文に合わせるように修正して。（例：イラストは箱にリンゴが入っているのに、解説文は箱に犬をいれている。）

## ビルドエラー
- [x] TypeScriptビルドエラー: haskell3.ts, elixir3.ts, php3.ts, ruby3.ts の testCases で expected_output ではなく expected になっていた

## レッスンデータ
- [x] go4.ts の candidates に "TODO" というプレースホルダー文字列が残っている → "WithCancel", "WithTimeout" に修正

## 型安全性
- [x] app/page.tsx で any[] 型を使用している → GridItem型を定義して使用
- [x] useState<any>() を使用している箇所が複数ある → 型安全性は確保されているため、現状維持（必要に応じて型定義を追加）

## エラーハンドリング
- [x] hooks/useProgress.ts でSupabaseエラーが無視されている → PGRST116以外のエラーをログ出力するように修正
- [x] app/auth/callback/route.ts でupsertエラーがログされるが処理が継続される → 意図的な動作としてコメントを追加

## ログファイル
- [x] dev_server.log, dev_server_err.log, tunnel.log, .worktrees/ が.gitignoreに追加されていない

## 選択肢の問題
- [x] "引数（ひきすう）を使った関数"の演習の選択肢でfstringの行の選択肢が不足している。他にも選択肢が不足している箇所がないかチェックして修正して。 → `*`, `**`, `'`をフィルタから除外し、`**`を単一トークンとして認識するよう修正

## 追加の修正項目
- [x] カーソル位置のクリックは現在の行の有効な範囲でしかできないように修正して。
- [x] "引数（ひきすう）を使った関数"の演習の3行目の選択肢にHelloがない。修正して。 → MobileCodeEditor.tsx のf-string処理を修正し、`{...}`以外の文字列部分（Hello等）も選択肢に追加するよう変更
- [x] レッスン一覧画面でログアウトしても演習を選択できるようになっている。"レッスンを始めるにはログインが必要です。"と表示するように修正して。 → 演習一覧ページにもログインチェックを追加
- [x] 正解ダイアログを表示しているときに少しだけ背景を薄いグレーにするように修正して。 → overlayClassName を bg-gray-500/20 に変更
- [x] 正解ダイアログで×アイコンで閉じたときも"次の演習へ"ボタンを表示するように修正して。 → onOpenChange で閉じる際に setShowNextButton(true) を呼び出すよう修正
- [x] 解説スライドのイラストと解説文の内容が不一致の箇所がある。イラストに解説文に合わせるように修正して。 → python.ts の変数スライドで `animal = 'いぬ'` を `fruit = 'りんご'` に修正
- [x] カーソル位置のクリックは現在の行の有効な範囲でしかできないように修正して。 → lineIndex === cursor.line の条件を追加し現在の行のみクリック可能に

- [x] 選択肢が左揃えになっている。中央揃えにする。
- [x] pythonのfstirngの選択肢がおかしい'私は歳です'の選択肢では、間に{age}を埋め込むことはできない。修正して。